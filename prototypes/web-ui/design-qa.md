# UI 检查记录 · 2026-09-10

本记录替代旧“同步拓扑视觉验收”。用户否定了旧实现的还原程度，并同意调整信息结构：首页管理全部实例，拓扑放在具体任务详情。当前保留第 2 套的配色、侧栏和配置检查器风格，不将新版布局描述为参考图的像素级复刻。

## 浏览器验证

运行 `node prototypes/qa/verify-web-ui.mjs`（先在 `prototypes/web-ui` 启动 `python -m http.server 4173 --bind 127.0.0.1`）。

实际通过演示登录进入页面，43 项检查通过，未收集到脚本或资源错误。结果见 [qa/results.json](qa/results.json)。

覆盖：三条任务各自的实例、库表、事务和日志筛选；8.0 同时担任源端和目的端；关联任务筛选；详情刷新、返回与切换；未知任务与草稿状态；搜索无结果；演示暂停/恢复计数；深浅色；移动导航；键盘切换日志标签；退出后演示页面保护。

视口：桌面 1440×1024、窄屏 700×1000、手机 390×844，deviceScaleFactor=1。窄屏表格允许自身横向滚动，页面没有整体横向溢出。详情内容较多时纵向滚动。

## 视觉证据与修正

参考图： [reference-topology.png](reference-topology.png)，1487×1058。
新版截图：
- [深色首页](qa/home-dark.png)、[浅色首页](qa/home-light.png)、[任务列表](qa/tasks.png)
- [订单任务](qa/detail-orders.png)、[用户任务](qa/detail-users.png)、[库存任务](qa/detail-inventory.png)
- [手机详情](qa/detail-mobile.png)、[窄屏首页](qa/home-narrow.png)、[登录页](qa/login.png)
- [样图与新版对照](qa/style-comparison.jpg)：缩放至同等宽度，比较颜色、区域层级和字体；首页与详情结构按新的产品要求调整。

截图检查后修正了桌面端误显手机菜单按钮的问题；窄屏下也保留所选源端/目的端的配置面板。新版没有用一个固定拓扑代替全部任务。

## 边界

这是交互样稿：实例状态、任务和日志均为本地示例，未连接真实后端。登录使用前端演示状态，不满足生产鉴权；暂停/恢复仅修改内存中的样稿数据。账号、路径和任务创建尚未接入，操作时显示提示。此前报告中的“无 P0/P1/P2、还原通过”不能作为新版或正式系统的质量结论；视觉是否满意仍以用户审阅为准。
